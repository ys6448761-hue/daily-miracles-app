---
name: Weekly Summary Pipeline
description: 주간 대화를 종합 정리하는 파이프라인
version: 1.0.0
trigger: manual | scheduled
schedule: "0 18 * * FRI"  # 매주 금요일 18:00
---

## 개요
일주일간의 대화를 수집하여 통합 주간 리포트를 생성합니다.

## 파이프라인 흐름

```
┌─────────────┐    ┌───────────────────┐    ┌──────────────────┐
│  입력 수집   │───▶│    Summarizer     │───▶│Decision Extractor│
│ (대화 로그)  │    │   (주간 요약)      │    │  (결정 사항)      │
└─────────────┘    └───────────────────┘    └──────────────────┘
                                                      │
                                                      ▼
┌─────────────┐    ┌───────────────────┐    ┌──────────────────┐
│ 최종 리포트  │◀───│   Report Merger   │◀───│ Action Extractor │
│   (출력)    │    │   (통합 정리)      │    │  (할 일 목록)     │
└─────────────┘    └───────────────────┘    └──────────────────┘
```

## 단계별 상세

### Stage 1: Summarizer
```yaml
agent: summarizer
input:
  text: "{{conversations}}"
  type: weekly
output: weekly_summary
```

### Stage 2: Decision Extractor
```yaml
agent: decision-extractor
input:
  text: "{{conversations}}"
output: decisions_list
```

### Stage 3: Action Extractor
```yaml
agent: action-extractor
input:
  text: "{{conversations}}"
output: action_items
```

### Stage 4: Report Merger (내장)
```yaml
merge:
  - weekly_summary
  - decisions_list
  - action_items
template: weekly-report-template
output: final_report
```

## 입력 스키마

```yaml
conversations: string  # 주간 대화 전체 텍스트
date_range:
  start: "2024-12-23"
  end: "2024-12-28"
team: string (optional)  # 팀명
```

## 출력 형식

```markdown
# 📊 주간 리포트
> 기간: {{date_range.start}} ~ {{date_range.end}}
> 생성일: {{generated_at}}

---

## 📋 주간 요약
{{weekly_summary}}

---

## ✅ 결정 사항
| # | 결정 내용 | 관련자 | 영향도 |
|---|----------|-------|-------|
{{#each decisions}}
| {{id}} | {{content}} | {{participants}} | {{impact}} |
{{/each}}

---

## 📝 Action Items
| # | 업무 | 담당자 | 기한 | 우선순위 |
|---|------|-------|------|---------|
{{#each actions}}
| {{id}} | {{task}} | {{assignee}} | {{deadline}} | {{priority}} |
{{/each}}

---

## 📈 다음 주 예고
{{next_week_preview}}

---
*🤖 Generated by Weekly Summary Pipeline*
```

## 사용 예시

### CLI에서 실행
```bash
claude pipeline run weekly-summary \
  --input conversations="이번 주 슬랙 대화..." \
  --date-range "2024-12-23,2024-12-28"
```

### 에이전트에서 호출
```
@weekly-summary 이번 주 대화 정리해줘
```

## 설정 옵션

```yaml
options:
  include_statistics: true    # 통계 포함
  highlight_blockers: true    # 블로커 강조
  send_notification: false    # 슬랙 알림
  export_format: markdown     # markdown | json | pdf
```

## 에러 처리

| 에러 | 처리 방법 |
|-----|----------|
| 입력 없음 | "이번 주 대화 기록이 없습니다" 반환 |
| 파싱 실패 | 원문 그대로 요약 시도 |
| 에이전트 실패 | 해당 섹션 스킵 후 계속 |
