---
name: 일일 운영 자동화 파이프라인
description: 매일 자동 실행되는 운영 작업 (메시지 발송, 리포트, 백업)
version: 1.0.0
trigger: cron
schedule: "0 6 * * *"
---

# 일일 운영 자동화 파이프라인

매일 자동으로 실행되는 운영 작업을 정의합니다.

## 일일 스케줄

```
06:00 ──┬── 데이터베이스 백업
        │
08:00 ──┼── 아침 응원 메시지 발송
        │
12:00 ──┼── 중간 리포트 생성
        │
18:00 ──┼── 일일 피드백 수집
        │
20:00 ──┼── 저녁 응원 메시지 발송
        │
23:00 ──┴── 일일 종합 리포트
```

## 작업별 상세

### 06:00 - 데이터베이스 백업

| 항목 | 설명 |
|------|------|
| 스크립트 | `scripts/backup-database.ts` |
| 대상 | PostgreSQL (Render) |
| 저장 위치 | Cloud Storage |
| 보관 기간 | 30일 |

```bash
# 수동 실행
npx ts-node scripts/backup-database.ts
```

### 08:00 - 아침 응원 메시지 발송

| 항목 | 설명 |
|------|------|
| 스크립트 | `scripts/send-messages-batch.ts --type=morning` |
| 대상 | 활성 소원이 (7일 이내 시작) |
| 채널 | 카카오 알림톡 |
| 템플릿 | `prompts/content/message-morning.md` |

**발송 대상 쿼리**:
```sql
SELECT * FROM wish_entries
WHERE status = 'active'
AND CURRENT_DATE - start_date <= 7
AND day_number <= 7
```

**발송 내용**:
```
🌅 좋은 아침이에요, {이름}님!

{Day N 아침 메시지}

오늘도 당신의 소원을 응원해요! 💫
- 하루하루의 기적
```

### 12:00 - 중간 리포트 생성

| 항목 | 설명 |
|------|------|
| 실행 주체 | `@comi-orchestrator` |
| 대상 | 오전 입력 건 통계 |
| 출력 | Slack/Discord 알림 |

**리포트 내용**:
- 신규 입력: N건
- 분석 완료: N건
- 발송 성공률: N%
- 특이사항: ...

### 18:00 - 일일 피드백 수집

| 항목 | 설명 |
|------|------|
| 실행 주체 | `@feedback-analyzer` |
| 소스 | 카카오채널, 이메일 |
| 출력 | `claude/feedback/` 업데이트 |

**수집 항목**:
- 별점 피드백
- 텍스트 피드백
- 문의 사항

### 20:00 - 저녁 응원 메시지 발송

| 항목 | 설명 |
|------|------|
| 스크립트 | `scripts/send-messages-batch.ts --type=evening` |
| 대상 | 활성 소원이 (7일 이내 시작) |
| 채널 | 카카오 알림톡 |
| 템플릿 | `prompts/content/message-evening.md` |

**발송 내용**:
```
🌙 오늘 하루도 수고하셨어요, {이름}님.

{Day N 저녁 메시지}

내일도 함께해요. 굿나잇! 🌟
- 하루하루의 기적
```

### 23:00 - 일일 종합 리포트

| 항목 | 설명 |
|------|------|
| 실행 주체 | `@comi-orchestrator` |
| 대상 | 전체 일일 통계 |
| 출력 | 이메일 + 대시보드 업데이트 |

**리포트 내용**:
```markdown
## 일일 운영 리포트 (2025-12-28)

### 요약
- 신규 소원이: 5명
- 분석 완료: 5건 (100%)
- 메시지 발송: 아침 12건, 저녁 12건

### 기적지수 평균
- 오늘: 74점
- 주간 평균: 72점
- 월간 평균: 71점

### 피드백
- 총 3건 (긍정 2, 중립 1, 부정 0)
- 평균 별점: 4.7/5.0

### 시스템
- 백업: 성공
- 에러: 0건
- 응답 시간: 평균 0.8초
```

## 에러 알림

| 심각도 | 조건 | 알림 채널 |
|--------|------|----------|
| Critical | 백업 실패 | 전화 + Slack |
| High | 발송 실패율 > 5% | Slack + 이메일 |
| Medium | 분석 지연 > 3분 | Slack |
| Low | 피드백 수집 실패 | 이메일 |

## 수동 실행

```bash
# 전체 파이프라인 수동 실행
node scripts/run-daily-operation.js

# 특정 작업만 실행
node scripts/run-daily-operation.js --task=morning-messages
node scripts/run-daily-operation.js --task=daily-report
```

## GitHub Actions 설정

```yaml
# .github/workflows/daily-scheduler.yml
name: Daily Operations

on:
  schedule:
    - cron: '0 21 * * *'  # UTC 21:00 = KST 06:00
    - cron: '0 23 * * *'  # UTC 23:00 = KST 08:00
    - cron: '0 11 * * *'  # UTC 11:00 = KST 20:00
    - cron: '0 14 * * *'  # UTC 14:00 = KST 23:00

jobs:
  run-operations:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Daily Operation
        run: node scripts/run-daily-operation.js
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          KAKAO_API_KEY: ${{ secrets.KAKAO_API_KEY }}
```

## 관련 파일

| 유형 | 파일 |
|------|------|
| 스크립트 | `scripts/backup-database.ts` |
| 스크립트 | `scripts/send-messages-batch.ts` |
| 스크립트 | `scripts/extract-user-data.ts` |
| 스킬 | `claude/skills/message-dispatcher.md` |
| 에이전트 | `claude/agents/feedback-analyzer.md` |
| GitHub | `.github/workflows/daily-scheduler.yml` |
