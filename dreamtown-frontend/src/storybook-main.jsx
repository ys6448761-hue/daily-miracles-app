import React from 'react';
import ReactDOM from 'react-dom/client';
import StorybookApp from './StorybookApp.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StorybookApp />
  </React.StrictMode>
);
